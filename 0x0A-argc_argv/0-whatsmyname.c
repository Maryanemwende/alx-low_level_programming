#include <stdio.h>

/**
 * main - takes CL arguments
 *
 * @argc: number of arguments passed to program
 * @argv: arrayr of strings containing the actual arguments
 * Return: 0 (Successful)
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
