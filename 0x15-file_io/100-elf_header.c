#include <fcntl.h>
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * @argc: argument count
 * @argv: array of pointers to arguments
 * Return: 0 (Success)
 */
int main(int argc, char **argv)
{
	(void) argc;
	(void) argv;

	return (0);
}
